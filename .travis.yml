language: python

python:
  - "3.6"
before_install:
  - sudo apt-get install gfortran
install:
  - pip install -r requirements.txt
  - pip install codecov pytest-cov
script:
  - travis_wait 50 pytest --cov=./
after_success:
  - codecov